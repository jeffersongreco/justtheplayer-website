{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "version": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "version"],
      "outputs": [".svelte-kit/**", "dist/**"]
    },
    "check": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {},
    "test:coverage": {},
    "size": {
      "dependsOn": ["build"]
    },
    "optimize-images": {
      "inputs": ["media/src/**"],
      "outputs": ["media/dist/**"],
      "cache": true
    }
  }
}
